// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("dval", "for dval support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("dval", "enable dval support", "no");

if (PHP_DVAL != "no") {
	EXTENSION("dval", "dval.c");
}

